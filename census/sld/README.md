# READ ME 

1. Use `download.ipynb` to download the data and convert shape files to parquet
2. Convert geometries in the parquet to hex with `to_hex.py`


   ```python to_hex.py --chamber lower```
   and
   ```python to_hex.py --chamber upper```
